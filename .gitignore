# Environment
.env.local
.env

# Secrets / local tooling
.mcp.json
.claude/
.playwright-mcp/

# Node
node_modules/
.next/
out/
tsconfig.tsbuildinfo

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
*.egg-info/

# Database — keep the seed snapshot in git so Railway has data on cold start,
# but ignore *other* .db files (dev copies, journals) and the event log.
db/*.db
!db/kazmedsim.db
db/*.db-journal
db/*.db-wal
db/*.db-shm
db/events.jsonl

# RAG source PDFs — too large for git, redistribute separately
docs/protocols/*.pdf
docs/protocols/.DS_Store

# OS
.DS_Store
